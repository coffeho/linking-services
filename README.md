# HR System

Учебный проект: система кадрового учета на Python и Tkinter.

## Текущее состояние

На начальном этапе проект представляет собой один файл `main.py`.

В этом файле сейчас находятся:

- GUI на Tkinter;
- работа с SQLite;
- авторизация пользователей;
- учет сотрудников;
- подразделения;
- должности;
- отпуска;
- приказы;
- переводы;
- отчеты;
- управление пользователями.

Дальше проект будет постепенно преобразован в структурированный инженерный проект согласно требованиям курса.

## Запуск

```bash
python main.py
```

## Системные зависимости

Для Linux/WSL может понадобиться установить Tkinter отдельно:

```bash
sudo apt install -y python3-tk
```

Если виртуальное окружение не создаётся, также установите поддержку `venv`:

```bash
sudo apt install -y python3.12-venv
```

## Тестовые аккаунты

- `admin / admin`
- `hr_manager / hr_manager`
- `accountant / accountant`
- `employee / employee`

## Smoke-тесты

Smoke-тесты фиксируют базовое поведение текущего монолита до рефакторинга.

Установка зависимостей для разработки:

```bash
python3 -m pip install -r requirements-dev.txt
```

Запуск smoke-тестов:

```bash
python3 -m pytest tests/smoke -v
```
